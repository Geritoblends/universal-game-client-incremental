[package]
name = "grid-driver"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
grid-protocol = { path = "../../crates/grid-protocol" }
tasksapp_allocator = { path = "../../crates/allocator" }
once_cell = "1.19"
