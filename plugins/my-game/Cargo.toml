[package]
name = "my-game"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"] # Compiles to .wasm

[dependencies]
tasksapp_allocator = { path = "../../crates/allocator" }
tasksapp_ecs_client = { path = "../../crates/ecs-client" }
